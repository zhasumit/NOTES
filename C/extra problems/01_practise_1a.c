// Write a program to find the area of the rectangle [hard coded]
#include <stdio.h>
int main()
{
    int length = 8, breadth = 5;
    int area = length * breadth;
    printf("The area of the rectangle is %d ", area);
    return 0;
}