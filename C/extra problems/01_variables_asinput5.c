// WAP tp take the inputs from the user assign it to the variables inthe program and return the sum of the values
#include <stdio.h>
int main()
{
    int a, b;
    printf("Enter the value of a : ");
    scanf("%d", &a);
    printf("Enter the value of b : ");
    scanf("%d", &b);
    printf("The sum of %d and %d is %d", a, b, a + b);
    return 0;
}