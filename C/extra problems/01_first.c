#include <stdio.h>
/*This is the first program in c programming 
which is damn awesome*/
int main()
{
    // variable is a box in memory whose values can be changed 
    int _tom_babu;
    int _TOM_babu; // variable are case sensitive
    
    // Also the variables must have a good name that gives a sense of meaning 
    int error_code;

    // storing the character must have a single quote ''
    char character = 'c';
    printf("Hello, I am learning C here");
    
    return 0; // return 0 means the program is success
}