#include <stdio.h>
int main()
{
    int a;  // variable decaration THis is a comment
    a = 34; // variable initialisation

    /* this is a mutiple line comment
    this could be written in multiple lines of code */

    float b;

    char c;

    double d;

    int salary = 40000;
    // variable declaration and initialisation at the same time is called dyanmic initialisation

    long int tar = 43434;

    long long int population = 7000000;
    population = 7000001;

    printf("population value:\n %i ", population);
    return 0;
}